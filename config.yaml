version: 1.0
agents:
  - name: client
    module: agents.client_agent # 👈 مسار مباشر
    entrypoint: client_agent
  - name: restaurant
    module: agents.restaurant_agent # 👈 مسار مباشر
    entrypoint: restaurant_agent

flows:
  default: [client, restaurant]