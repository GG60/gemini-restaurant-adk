version: 1.0
agents:
  - name: client
    module: agents.client_agent
    entrypoint: client_agent
  - name: restaurant
    module: agents.restaurant_agent
    entrypoint: restaurant_agent

flows:
  default: [client, restaurant]